package com.zuoqirun.lyricscompanion;

/** Optional cloud fallback. Implementations must own their endpoint and credentials. */
public interface CloudSpeechRecognizer extends LocalSpeechRecognizer {
}
