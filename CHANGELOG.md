# Lyrics Companion 更新日志

## 20260925-lyric-pipeline-and-boot-fixes

> 本次仅完成本地单元测试 + Android Lint；网易 eapi、咪咕 MRC 与酷我逐字三条新通道、源规则热修、缓存后台升级、单曲偏移记忆、开机自启与播放控制按键补建都需要在车机上联网复验（升级后旧缓存会在后台补拉逐字 / 罗马音，缓存体积也会变大），最低支持 Android 4.4。

- 新增咪咕音乐词库（issue #69）：搜索走 `jadeite.migu.cn` 的 searchAll，歌词走 `resourceinfo.do`，用 TEA 解密还原 MRC 逐字时间轴并换算成绝对毫秒；「默认匹配词库」「强制词库」「重新匹配」三处选择器都加了咪咕，咪咕播放器默认优先用咪咕乐库。
- 网易云歌词改走 eapi 加密通道（issue #55、#69）：从公开的 `music.163.com/api/song/lyric` 换成桌面端同款 `interface3.music.163.com/eapi/song/lyric/v1`（AES-128-ECB + MD5 摘要），降低风控与接口随时失效的风险；响应 `code` 非 200 时明确报错，不再当成"这首歌没有歌词"。
- 网易云匹配加严（issue #55）：搜索结果先剔除「歌手完全对不上」与「时长差超过 15 秒」的候选；标题只差一个显示后缀时，还要求歌手一致、时长差 2 秒以内且 Live / 伴奏 / 翻唱这类版本标记一致；用「歌名 + 歌手」搜不到时再用纯歌名搜一次，单次取 20 条候选。旧的网易歌词缓存目录同时换到 v3，避免沿用过时结果。
- 解析网易罗马音（issue #69）：以前请求了 `rv=-1` 却只读 `lrc/tlyric/yrc`，现在 `yromalrc`（逐字）与 `romalrc`（逐行）都会解析，逐字罗马音会先去掉字级时间标签再按行落地。
- 歌词行模型改为多语言扩展槽位（issue #69）：`Line` 从「一个翻译字段」改成 `extendedLyrics[]`（translation / romaji），缓存格式升到 v2 并兼容读取旧 v1 缓存；显示侧在没有翻译时自动用罗马音顶替翻译行，经典、纯净、Refined、紧凑与 PiPWindow 表现一致。
- 酷我补逐字通道（issue #69）：在原来的逐行 JSON 接口之前先试 `mlyric.kuwo.cn` 的 `lrcx=1` 通道，zlib 解压 + base64 + `yeelion` XOR 解码后，把酷我的往返偏移 `<off1,off2>` 换算成绝对逐字时间并单独缓存；该通道不可用时自动回落到原来的逐行歌词，不影响现有命中。
- 汽水音乐空结果不再永久固化（issue #48）：以前 share 页面没解析到歌词也会写 `_checked` 标记，导致这首曲子从此不再重试；现在只有真的拿到歌词才写标记，历史遗留的空标记按"未检查"处理，下次播放会重新拉取。
- 兜底阶段并发 + 每源重试 + 真取消（issue #69）：多词库兜底改为并行发起、仍按优先级取第一个有结果的源（手动词库 / 播放器同源优先的语义不变）；HTTP 层对 5xx 与 IO 错误重试两次并退避；切歌时直接 `disconnect()` 正在进行的请求，不必再等 10 秒读超时。
- 缓存内容自我升级（issue #69）：命中歌曲线缓存但缺逐字 / 翻译 / 罗马音、且该源本来就能提供时，在后台静默补拉一次，只有质量分更高才覆盖缓存，不需要手动清缓存。
- 支持 `[offset:]` 标签与单曲偏移（issue #69）：导入的歌词文件自带的 `[offset:]` 现在会生效（行时间与绝对逐字时间一起平移）；「显示参数 → 当前歌曲校正」可为这一首歌单独记一份 ±5 秒偏移，叠加在全局偏移与播放器校正之上，换歌自动读取对应记录。
- 本地歌词读取补齐（issue #62、#69）：除同名 `.lrc` 外也读同名 `.krc`（按 `krc1` 文件头识别并解码，含内嵌翻译与逐字）；UTF-16（有 BOM 与无 BOM 的零字节启发式判断）与 GB18030 都能读；单文件上限从 512 KB 提到 10 MB；支持 lx-music 的 `[awlrc:lrc,tlrc,rlrc,awlrc]` 侧车格式，按字段校验后解回逐字与罗马音。
- 歌词源规则可远端热修（issue #69）：新增 `public/lyric-rules.json`，客户端最多每 6 小时拉取一次，失败保留上次结果；可覆盖各源的域名、固定参数与两个解码开关。只接受已知平台域名、固定路由名与长度受限的参数，解析算法仍留在应用内。
- 修复升级后开机不再自启动（issue #66）：接收器在 `LOCKED_BOOT_COMPLETED`（直启阶段读不到凭据加密存储里的偏好）直接返回，改由 `BOOT_COMPLETED` / `USER_UNLOCKED` 与 3 秒后的补跑接管；`startRememberedFromSystem` 现在把 reason 与开关状态写进诊断，便于区分「开机广播没送到」和「没有记忆对象」。
- 修复锁定并穿透后播放控制按键点不动（issue #46）：悬浮窗 `addView` 返回时面板还没测量，`getWidth()` 为 0 会被跳过且永不补建；现在挂了布局监听，测量完成后按需补建或对齐播放控制条。
- 「指定应用」隐藏规则更容易自查（issue #61）：未授权「使用情况访问」时设置页持续显示醒目提示（不再只有一次 Toast），诊断行补上 `usageAccess=`，且只在判定变化时记录一条，避免每 600ms 刷屏。
- 设置页文案精简：歌词匹配、启动与交互、屏幕与样式等分组的长说明改回一行，不再复述与开关无关的细节。

## 20260919-per-style-parameters-and-preview

> 本次仅完成本地单元测试 + Android Lint；「按样式保存」的迁移效果（老安装升级后观感是否完全不变、逐样式微调是否顺手）与自定义颜色页预览的排版都需要实机确认，最低支持 Android 4.4。

- 显示参数按「屏幕 × 样式」各存一份（issue #39）：颜色与描边、字号（含歌名 / 上一句 / 下一句）、背景不透明度与封面参数、歌词显示行数、水平与垂直对齐、面板圆角、粒子消散与逐字擦除（含估算逐字）、圆形封面与碟片旋转、匹配中动画——在某个样式下调好的参数，切到别的样式不会再被带过去，也就不必来回重调。
  - 升级后观感不变：没调过的项继续沿用本屏原来的共用值，只有你在某个样式下真正改过的项才会为该样式单独存一份（这是迁移策略，不写任何"默认值快照"）。
  - 只拆「外观」类参数：样式选择本身、位置锁定与触摸穿透、歌词时间校正、缓存与隐藏规则这些与样式无关的行为设置仍按屏共用——按样式各存一份只会让用户改了一处、别处不生效。
  - 「显示参数」页标题现在显示「正在编辑：<屏> · <样式>」，并提供「把「<样式>」的参数应用到本屏其它样式」按钮（只复制你在当前样式下改过的项，没调过的项继续继承共用值）。
  - 位置与面板宽高本来就是按样式存的，这次与它们口径一致。
- 自定义颜色页新增页内实时预览（issue #24）：颜色页顶部多了一块小尺寸歌词预览（按当前编辑的区域：主屏 / 副屏 / 顶部歌词条 / 各附加屏），改歌词颜色、当前 / 非当前颜色、描边与背景时立刻能看到效果，不必回「总览」页确认；切换编辑区域时预览随之切换。显示参数页、紧凑 / Refined / 极简 / 顶部歌词条设置页原本就有预览，这次统一确认它们都是随改动即时刷新的。
  - issue 里"设置时保留悬浮窗（不 dismiss）"这个可选项没有做：设置页仍会暂时收起悬浮窗，页内预览已经能满足"改完立刻看到"的需求。

## 20260919-lyric-source-and-cache

> 本次仅完成本地单元测试 + Android Lint；内嵌歌词回退在真实车机/播放器（Poweramp 等不给 mediaUri 的情况）上的命中率、逐字估算在长音与拖腔上的偏差观感都需要实机确认，最低支持 Android 4.4。

- 新增「清除歌词缓存」入口（issue #20）：「显示参数 → 歌词缓存」里显示当前占用与条目数，并可一键清除全部歌词、翻译、逐字时间轴与歌曲匹配结果（缓存由所有屏幕共用，清除后下次播放重新联网匹配）。
- 默认缓存策略补上时间上限（issue #20）：以前只有「保留 30 天」会过期，默认的「按容量自动淘汰」在歌少、总量到不了容量上限时旧条目永不过期、匹配错了也一直命中。现在「按容量自动淘汰」与「保留 30 天」都带 30 天上限（读缓存时判定 + 写入时清理），只有「永久保留」不受限制。
- 播放器不给 `mediaUri` 时（如 Poweramp）也能读到内嵌歌词（issue #25）：`mediaUri` 缺失时，在用户已授权的本地歌词目录（SAF 或手动填写的路径）里按候选名同时寻找音频文件（`.mp3/.flac/.m4a/.aac/.ogg/.opus/.wav`），命中后读取其内嵌标签；只在 `mediaUri` 缺失时才启用这条链路，扫描上限（2000 项）与扩展名过滤保持不变，不给正常路径增加开销。
  - 源名区分「内嵌歌词」与「本地 LRC」，排查时能看出命中的是哪条链路；失败路径（找到音频但没有内嵌歌词等）继续写进「音乐状态与诊断」。
  - 诊断快照新增 `mediaUriPresent`：一眼分清「播放器没给路径」和「文件里没有标签」。
- 无逐字时间轴时可按本句时长估算逐字进度（issue #21）：普通 `.lrc` 只有行时间轴，原本整句一次性变色，于是同一台车上「有逐字时间轴的歌逐字变、只有行时间轴的歌整句变」。新增开关（默认关）打开后，按「本句已播放比例 × 文本宽度」由白到蓝逐字推进，经典 / 紧凑 / HUD / 顶部条 / Refined 观感统一；长音、拖腔与行内停顿会让估算提前或滞后，只影响观感、不影响歌词同步，所以默认关闭。

## 20260919-source-rules-and-stuck-fallback

> 本次仅完成本地单元测试 + Android Lint；白名单在车机桌面 / 识别不到前台时的实际表现、忽略名单对具体播放器的影响、以及卡住回退在真实 AVRCP 车机上的触发时延都需要实机确认，最低支持 Android 4.4。

- 隐藏规则支持白名单（issue #43）：「指定应用」现在每块屏都能独立选择方向——**黑名单**（进入名单内的应用时隐藏，原行为）或**白名单**（只在名单内的应用里显示）。白名单下**识别不到前台应用也按隐藏处理**：很多车机桌面是常驻底层、根本不发前台事件，黑名单永远命中不了它，白名单不需要识别桌面就能做到「只在桌面显示」。唯一的例外是完全没有「使用情况访问」权限（可检测的情况）——那意味着永远读不到前台包名，白名单会把歌词一律藏掉，所以按不生效处理，设置页与诊断日志都会提示去授权。
- 一键「只在桌面显示」（issue #28）：「指定应用」里新增该按钮，自动把白名单设成系统桌面（按 `CATEGORY_HOME` 查出的桌面包名，可能不止一个），并为主屏与副屏启用白名单。
- 隐藏规则的自查更方便：诊断日志里的可见性行补上前台包名与两屏的规则方向（`visibility mainHidden=… foreground=… 规则=主屏白名单/副屏黑名单`），白名单「foreground=空 ⇒ 隐藏」这次判定是什么原因可以直接看出来。
- 支持屏蔽指定应用的媒体元数据（issue #35）：新增「忽略这些应用的媒体元数据」名单（应用选择器可搜索多选 + 摘要），被忽略的应用不再更新歌词状态，车机自带媒体中心、导航抢歌词时直接忽略它；另提供「加入蓝牙音频（`com.android.bluetooth`）」快捷项——蓝牙 AVRCP 与 CarPlay / 媒体会话互相抢占时，也可以只留一条通道。只影响这些应用发布给歌词伴侣的元数据，不影响它们自己的播放。
- 匹配歌词长时间不滚动时回退实时歌词（issue #44）：蓝牙等通道偶尔会匹配到一条一直停在第一句的时间轴。现在当「播放进度在推进、当前行超过 25 秒没有推进、且播放器的实时歌词在此期间变化过至少 2 次」时，放弃这条时间轴、改用播放器实时歌词，并写一条诊断（`lyric fallback reason=stuck lineStartMs=… elapsed=…s liveLyricChanges=… lines=…`）；反过来，实时歌词自己超过 15 秒不动（纯音乐、AVRCP 只发一次元数据）时仍然保留原时间轴。单行时间轴、或时间轴总时长与歌曲时长严重不符（>3 倍或 <1/3）的匹配直接视为不可用。开关在「歌词来源与校准 → 歌词匹配」里（默认开启），关掉时行为与改动前一致。

## 20260919-appearance-features

> 本次仅完成本地单元测试 + Android Lint；圆角/碟片旋转/匹配动画/时间段配色的实际观感（圆角百分比在 4.4 小屏上的分寸、旋转在弱车机上的流畅度与耗电、发散小点的位置是否压到进度条、深色时段切换时机）都需要实机确认，最低支持 Android 4.4。

- 面板圆角可调（issue #37）：新增「面板圆角（0 = 直角）」，0%–50% 按面板短边百分比计算、按屏存储。0% 就是直角矩形，配合「背景不透明度」100% 可以把面板当成一整块实心色板，完全盖住后面的原车界面；没调过时沿用当前样式原本的圆角（经典约 7%、Refined / AMLL 约 8%、紧凑 18%、纯净 / 极简 22%），升级后观感不变；全屏样式保持贴边不加圆角。
- 圆形封面碟片旋转（issue #22）：新增「圆形封面碟片旋转」与「转一圈的秒数」（3–60 秒，默认 20 秒 ≈ 3 转/分，接近黑胶）。角度按播放进度推导，暂停即停、拖动进度会跟着跳，不会因为系统时钟在暂停后继续转。哪些封面能转：Refined 关掉「方形专辑封面」即为圆形；紧凑与 AMLL 原本只有圆角方形封面，新增「圆形封面（紧凑 / AMLL）」开关，打开后它们也是正圆，旋转随之生效。旋转按屏幕刷新率（约 60 fps、与 vsync 对齐）出帧，比 30 fps 固定延迟明显顺滑；只有真的会转的样式才为它保持帧率，方形 / 圆角封面开着开关也不额外耗电，觉得费电或卡顿可以关掉。
- 顶部歌词条独立「下一句字号」（issue #19）：顶部歌词条详细设置 →「布局与字号」新增该项（45%–160%）。没在这一页调过之前沿用主屏的下一句字号（升级后观感不变）；调过之后只影响顶部条，之后调主屏不再带着顶部条一起变。
- 「正在匹配歌词」动画化（issue #45）：匹配期间歌名照常显示，面板底部给一排从中心向两侧发散的小点，不再是孤零零一行静止文字。新增「匹配歌词时显示动画」与「匹配超过多久才显示动画」（0–10 秒，默认 3 秒，避免瞬间匹配成功时闪一下）；动画出现时这个位置显示歌名，通知栏同步改成「歌名 · 正在匹配歌词」，不再出现"面板显示歌名、通知说正在匹配"的不一致；关掉动画即回到原来的静止文字，动画期间保持约 30 fps。
- 按时间段自动切换深浅色（issue #34）：「跟随系统 / 白天 / 夜晚」之外新增**可选的**「按时间段自动切换深浅色」开关——打开后，在设定的深色时段（默认 19:00 → 07:00，跨零点自动处理，起止相同表示不启用该时段）内用夜晚配色、其余时间用白天配色；**关掉就完全按原来的模式判断**（跟随系统即由系统深浅色决定）。悬浮歌词的自动配色与设置页主题都会跟着换，运行中每半分钟对一次表，到点自动切换。

## 20260919-overlay-hints-and-duplicate-screens

> 本次仅完成静态检查与本地单元测试 + Android Lint；穿透模式的 × 手柄改为两段式解除、同屏重复提示与说明文案都只看代码推演，实际手感（手柄大小、两段式提示是否够醒目）与车机投屏通道的 Display 命名规律仍需实机确认，最低支持 Android 4.4。

- 锁定/穿透的 × 手柄不再一点就静默解除（issue #36）：第一次点击只把按钮变成醒目的橙色并提示「再点一次解除位置锁定与触摸穿透」，3 秒内再点一次才真正解除；点到别处或超时自动恢复原样，「关闭按钮」设为弱化/自动隐藏时也会按原设置继续淡化。解除时补一条 Toast「已解除位置锁定与触摸穿透」，与附加屏幕长按解除的提示保持一致。
- 说明文案写清互斥关系与出路（issue #36）：「锁定位置并穿透（保留播放控制按键）」和「锁定并触摸穿透」各自补了说明，写明二者二选一、× 手柄怎么用；同时修正设置页的行为不一致——从这里打开「锁定并触摸穿透」现在和悬浮窗快捷菜单一样把「锁定位置并穿透」关掉（并同步开关外观），因此关闭穿透后歌词会真正恢复可拖动，不会出现"开关显示关着、窗口却还是锁死"的状态。
- Android 12 及以上穿透会让歌词看起来更暗，现在会说明原因（issue #32）：系统要求穿透窗口的不透明度低于 80%（当前实现 79%），整窗连同歌词一起变暗是系统限制而不是设置被改；开启锁定/穿透时会用 Toast 说明，设置页两条说明里也写清了，Android 11 及以下不受影响。
- 诊断日志补充穿透窗口的 alpha（`touch through … alpha=0.79`），用户回传快照即可确认变暗是不是这条限制引起的。
- 同一块物理屏被车机暴露成多个 Display 时给出提示（issue #23）：有的车机把一块屏开成多个投屏通道（例如 `…ScreenProjection_0` 与 `…ScreenProjection_1`），两块都开启只会叠在同一块屏上画两层歌词。现在「其它屏幕同时显示」里，疑似同一块屏的那一行会标注「可能是同一块屏（与「…_0 · ID 3」名称只差通道编号 _0 / _1）」，列表末尾在已开启的屏之间检测到重叠时再补一条说明（叠两层只是更粗更亮、还白跑一遍渲染，建议只留一块）。
  - 判定只用屏幕名、分辨率与像素密度：名字去掉尾部通道编号（`_0` / `#1` / `-2` / `（3）`）相同，或名字完全相同，算强信号；名字相近且分辨率、像素密度一致算弱信号。只提示，不合并、不隐藏、不改任何设置——想两块一起开仍然可以。
  - 真正附加窗口时，若新屏与已经在显示歌词的屏疑似同一块，诊断日志会记一行（`extra slot N looks like a screen already showing lyrics（…）`），重叠渲染不再是"白跑一遍却没人知道"。

## 20260919-lyric-layout-and-session-fixes

> 已完成本地单元测试（224 项通过、7 项跳过）、Android Lint 与签名 Release 构建（versionCode 1789402625、versionName 20260919-lyric-layout-and-session-fixes）；本次改动落在渲染与会话的核心路径，经典/纯净样式的行位与字号观感、安卓 10 车机手动歌词目录、以及蓝牙 AVRCP 与 CarPlay 同时在线的车型表现仍需实机确认，最低支持 Android 4.4。

- 「歌词显示行数」在经典样式下真正生效（issue #26）：3 行＝上一句 / 本句 / 下一句，2 行＝本句 + 下一句，1 行＝只画本句并落在可用区中间；不足 3 行时空出来的行位一并收回，不再留一块空白。
- 经典样式新增「上一句字号（经典样式）」（45%–160%，默认 55%，等于原来写死的 12dp，升级后观感不变）。
- 经典样式的行距改为跟着字号走（issue #38）：调大「歌名与歌手字号」时把下面几行推开，而不是把整块歌词一起缩小；歌名不再"怎么调都不变大"，上一句也不必再比下一句小一大截。
- 新增「内容垂直对齐（经典 / 纯净）」，按屏存储、默认跟随样式：面板贴到屏幕顶端时选「顶部」，歌词就真正顶格，不再固定留一段空白（issue #41）。
- 关掉「显示播放器与歌词来源状态行」时，经典样式把状态行占的那一段也收回去，不再只是不画字却留着位置（issue #41）。
- 修复已擦除的字整句回闪（issue #27）：只有播放位置真的往回跳（超过 1.2 秒，判定为用户 seek）才重新显示已擦除的字，播放器上报位置的回抖不再让主屏 / 副屏 / 顶部歌词条同时闪一下。
- 修复多行歌词下消散完的行「复活」（issue #40）：窗口里所有已经唱过的行都走消散路径，消散完的上一句上移一行后不会再被整句重画；往回拖（seek）时消散记忆清空，重新唱到的行完整显示，不会留下空洞（issue #29 的"更早的行生硬消失"也一并消除）。
- 修复副屏「显示上一首 / 播放暂停 / 下一首按钮」开关无效（issue #30）：读取改为与写入一致的按屏键，旧安装里已保存的设置继续生效。
- 纯净等窗口样式的首句行位固定（issue #31）：窗口永远按「歌词显示行数」摆同样多的行位，本句始终落在同一行，歌曲开头与结尾不足的行位留空；首句不会再被顶到面板最上面压住车机的车道信息。
- 安卓 10 车机「手动填写歌词目录路径」读不到 .lrc（issue #33）：补齐 `READ_EXTERNAL_STORAGE`（Android 13 起不再申请）与 `requestLegacyExternalStorage`，填写路径保存后会申请运行时权限，被拒绝时明确提示「已保存，但当前系统不允许按路径读取」，并给出改用「选择本地音乐目录」的出路；Android 11 及以上直接说明按路径读 .lrc 不可行，不再让用户以为设置生效了。
- 本地歌词搜索不再静默失败：目录不存在、权限不足、分区存储拒绝、扫描达到 2000 项上限、候选文件读不出、没有匹配到候选名，都会写进「音乐状态与诊断」（含当前读取权限与分区存储开关状态），安卓 10 上可直接区分「权限没给」和「分区存储拒绝」。
- 播放通道仲裁（issue #42）：蓝牙 AVRCP 与常规媒体会话（CarPlay 等）同时上报时只采纳一个来源——按是否有歌名、是否在播、是否有进度与时长打分，且当前来源在持续上报期间不会被另一条通道顶掉，两条通道来回抢的来源切换不再反复重置歌词时间线（这就是"歌词一闪一闪"的成因）。
  - 交接规则：当前来源真的停止上报满 3 秒、或进度时间戳超过 5 秒不再更新（暂停/掉线）、或挑战者明显更完整（分数差 ≥ 30）时才换源；换源原因与抖动都会写进诊断日志（`active source …` / `active source flapping from=… to=…`），方便在车机上判断是谁在抢。没有元数据的写入不再能顶掉有歌名的来源。
  - 若实机上出现"该让位却不让位"，把 `SessionChannelArbiter.HOLD_MS`（当前 3 秒）调小即可；"暂停时被蓝牙抢走"则调大 `PROGRESS_STALE_MS`（当前 5 秒）。

## 20260914-multi-display-alignment-and-embedded-lyrics

> 已完成本地单元测试（含新增用例）、Android Lint 与签名 Release 构建，并同签名保留数据覆盖安装到手机确认版本；多显示器热插拔、蓝牙综合识别的实际车型表现、以及对齐与裁切改动的观感仍需实机确认，最低支持 Android 4.4。

- 副屏不再只有一块：在「副屏与位置 → 其它屏幕同时显示」里可以给任意多块显示器各自开启歌词（issue #17）。驾驶副屏用紧凑双行、HUD 用另一种样式可以同时显示，每块屏都有自己的样式、尺寸、字号、透明度、位置、颜色与隐藏规则；副屏仍是原来的那一块，旧配置原样保留。
- 每块附加屏幕的设置存在自己的配置文件里（槽位 2、3……），因此改动 HUD 不会带着驾驶副屏一起变；某块屏临时拔掉也不会挤掉其它屏的槽位。设置页标题会显示正在编辑哪块屏，颜色页的区域下拉也会列出每块附加屏幕。
- 新增「歌词水平对齐（经典 / 紧凑 / 纯净）」（issue #18）：可选跟随样式、居中、居左；本句、翻译、上一句与下一句共用同一列与同一对齐，消散与逐字擦除的擦除边界也跟随该对齐，不会出现本句居左、翻译居中的错位。
- 顶部歌词条的粒子消散与主屏分开：「本句结束粒子消散」「粒子量」「逐字歌词及时擦除」「上一句不透明度」现在有顶部条自己的设置（顶部歌词条详细设置 → 歌词粒子与消散），调整主屏不再带着顶部条一起变，反之亦然；首次在该页调整前沿用主屏当前设置，升级后观感不变。
- 每块附加屏幕可单独选择样式：「主屏 / 副屏 / 其它屏幕样式」会为每块已开启的屏幕列出样式下拉，以及该样式对应的详细设置入口与显示参数入口；样式与详细参数都存在该屏自己的配置里。
- 副屏位置摇杆在多屏开启时可选择目标屏幕：摇杆上方出现「摇杆调整的屏幕」，可选副屏或任意一块附加屏幕，选择会记住；只有副屏时界面与原来完全一致，摇杆仍只移动副屏。
- 修复紧凑样式「本句结束粒子消散」把当前句裁在上一句宽度的问题（issue #15）：消散结束后擦除前沿不应再把新句限制在旧句右端，现在旧句一旦完全消失就撤掉该裁切，新句比旧句长时不再被切掉尾部。
- 新增「从歌手栏综合识别歌名（蓝牙/未知通道）」开关（issue #16）：部分手机音乐 App 把实时歌词写进「歌名」栏、把「歌名 - 歌手」写进「歌手」栏，开启后先从歌手栏解析歌名再匹配词库，歌名栏原文作为实时歌词显示；解析不出时完全保持原样。分隔符支持 `—` `–` `-`、`·`、`|`、`：`、`/`，其中单独的 `-` 与 `/` 只在含中文时才当作分隔符，避免把 A-Lin、AC/DC 这类歌手名拆开。
- 悬浮窗隐藏规则的应用选择器现在也列出系统桌面（launcher3 等声明 CATEGORY_HOME 的应用），标为「桌面」，因此可以设置「桌面隐藏歌词」（issue #14）。
- 本地歌词支持直接读取音频文件的内嵌歌词（FLAC Vorbis `LYRICS`/`UNSYNCEDLYRICS`、MP3 ID3 `USLT`/`SYLT`、M4A `©lyr`、OGG/Opus `OpusTags`），优先级为同目录 `.lrc` > 内嵌标签 > 已授权目录搜索，避免内嵌旧歌词覆盖手工修正过的 `.lrc`；无时间轴的内嵌歌词按固定节奏显示（issue #11）。

## 20260913-lyric-dissolve-and-word-erase

> 已完成本地单元测试（196 项通过、7 项跳过）、Android Lint、签名 Release 构建，以及同签名保留数据覆盖安装与版本确认；消散、逐字擦除、句尾重音与紧凑歌词滚动的实际观感仍需实机确认，最低支持 Android 4.4。

- 「上一句结束粒子消散」更名为「本句结束粒子消散」：消散对象明确为刚刚唱完的本句，从左到右逐字崩解并掉渣，最后一个字与时间轴末尾对齐。
- 新增「逐字歌词及时擦除」：逐字歌词唱完一个词就消散一个词，还没唱到的文字保持完整；只有单行歌词时，本句全部消散后才显示下句。
- 新增「粒子量」设置（20%–300%，默认 100% 与既有观感逐颗一致）；「粒子量」与「逐字歌词及时擦除」只在「本句结束粒子消散」开启时显示并生效，关闭主开关时一并隐藏。
- 消散覆盖全部样式：经典、Refined、AMLL、纯净、PiPWindow、布局编辑器，以及紧凑歌词和顶部歌词条。紧凑单行没有“上一句”行，改为在本句原位逐字崩解、新句从左到右被擦出来，不再提前整句显示。
- 单行过长向右滚动（跑马灯）时同样逐字擦除：擦除边界跟随滚动坐标，不会与文字错位或回闪。
- 句尾长音重音改为只加辉光、不再重绘字形（对齐 Refined Now Playing 的做法）：不再出现相邻字重影，描边与阴影/辉光设置也不再丢失。
- 修复歌词文本与歌名或歌手完全相同时被误判为元数据、丢失歌词填充色与描边的问题（issue #6）：歌词绘制路径不再用字符串相等判断元数据，颜色与描边判定只在元数据路径生效。
- 修复已擦除的字回闪：擦除状态改为跟随正在播放的那一行，滚动查看歌词、句末空档与暂停都不会让已唱的字重新出现；滚动查看其它行时依然是完整歌词，不会出现空洞。
- 修复拖长音重音在歌词末尾留下一块缺口的问题：辉光改用文字阴影绘制，不再使用模糊遮罩——模糊遮罩会强制生成边界只包住字形的离屏图层，比字形宽得多的辉光会在图层边界被硬边裁掉。

## 20260913-traditional-chinese-lyric-lookup

> 本次仅完成静态检查，新增测试用例尚未运行；本地单元测试、Android Lint、构建与实机验证待安排。不同繁体歌曲名、歌手名、文件名及词库命中准确性仍需验证，最低支持 Android 4.4。

- 歌词检索新增繁体中文标题和歌手的简体候选：Spotify 等播放器上报繁体元数据时，会保留原始信息和媒体 ID 的首次查询，再尝试简体标题、简体歌手及两者组合，提升大陆词库的匹配机会。
- 本地文件名被拆分为“歌手 - 歌名”候选后，也会追加对应的简体候选；原始查询优先，避免覆盖已有可正确匹配的繁体词库结果。

## 20260913-overlay-session-and-local-lyrics-fixes

> 本次仅完成静态检查，新增测试用例尚未运行；本地单元测试、Android Lint、构建与实机验证待安排。毛玻璃、比亚迪媒体会话和无目录选择器车机仍需实机确认，最低支持 Android 4.4。

- 顶部歌词的“毛玻璃”不再启用部分 ROM 会扩散到整块屏幕的系统级背景模糊，改为局部半透明玻璃背景，避免遮挡桌面和其它应用。
- 多个媒体会话并存时，带歌名的暂停会话现在优先于无元数据的播放空壳会话；空壳会话不会再阻止通知兜底读取正常播放器信息。
- 车机没有目录选择器时，点击本地歌词目录会先给出明确说明：只需将 `.lrc` 与歌曲放在同一目录即可匹配，无需目录授权。
- 新增手动填写本地歌词目录路径：没有系统文件选择器的车机可指定一个可访问目录，应用会在该目录及子目录查找匹配的 `.lrc`；该路径不会写入配置分享码。
- 上一句歌词增加主屏、副屏独立的不透明度设置，范围 0–100%；设为 0% 时仍保留布局占位，可用于调整歌词组位置。
- 主屏、副屏均可分别隐藏播放器与歌词来源状态行、以及进度条；进度条开关覆盖经典、Refined、AMLL、PiPWindow 和布局编辑器样式。
- README 补充比亚迪 DiLink 4.0 系统媒体中心的适配边界与可行替代方案。

## 20260906-notification-fallback-and-offline-lyrics

> 本次仅完成静态检查，新增测试用例尚未运行；本地单元测试、Android Lint、构建与实机验证待安排。不同播放器通知格式、断网重播和 U 盘场景仍需验证，最低支持 Android 4.4。

- 新增音乐通知识别兜底：系统媒体接口没有有效歌曲时，尝试读取标准标题、正文、副标题、大文本、多行文本及可展开的自定义通知文字；已有正常媒体信息优先，本次不新增 AIDL 适配。
- 补充系统音乐、Samsung Music、VLC、Poweramp、Musicolet 和 AIMP 的播放器识别。通知未提供播放进度时明确提示，不根据通知发布时间伪造歌词同步。
- 新增歌曲匹配结果缓存，完整保存歌词、翻译与逐字时间轴；相同歌曲信息命中缓存后，跳过联网搜索和歌词请求。手动重新匹配会跳过这层缓存。
- 缓存默认采用按容量淘汰方式，上限默认 128 MB，仍可选择永久保留或保留 30 天；已有显式设置继续生效，切换保留方式时尝试复用原目录缓存，写入使用原子文件保护。
- 改进 U 盘与本地歌曲适配：通知提供可访问的完整文件路径时，开启本地歌词后可读取同目录同名 LRC；目录匹配补充清理后的文件名候选，并修复路径中的加号、百分号被错误解码的问题。只有文件名时仍无法推断 U 盘挂载路径。

## 20260906-kuwo-car-lyrics

> 已完成本地单元测试（148 项通过、7 项跳过）、Android Lint、签名 Release 构建，以及手机同签名保留数据降级安装和启动验证；酷我两版的实际播放、切歌和进度拖动仍待实机验证。

- 酷我公版、极简版及应用名称包含“酷我”的播放器默认使用酷我乐库，用户手动设置的单应用乐库仍优先。
- 酷我歌词优先使用播放器提供的歌曲 ID，失败后再搜索；搜索支持多个候选和纯歌名回退，过滤伴奏、翻唱及明显时长不符的结果，减少无法匹配和版本误匹配。
- 接入酷我公版通过媒体会话提供的整首歌词及行时间轴；校验歌词头信息并隔离切歌时的旧载荷，避免较晚返回的网络结果覆盖播放器歌词。本地 LRC 和手动重匹配保持优先。
- 诊断日志区分酷我接口业务错误和空歌词，便于排查 HTTP 请求成功但歌词查询失败的问题。

## 20260831-settings-and-overlay-reliability

> 已完成本地单元测试、Android Lint、Release 构建，以及 Android 11 手机上的同签名覆盖安装与冷启动验证；车机副屏、开机恢复和各播放器组合场景仍建议继续实机反馈。

- 设置主页新增“精简模式 / 完整模式”：精简模式只保留主屏、副屏、顶部歌词条开关和尺寸/字号/透明度等日常参数；完整模式重构为“总览、显示、歌词、高级”四个分类，并按主屏、副屏、顶部歌词条提供直达入口，新手可按三步引导完成首次配置，熟悉后也能快速找到颜色、样式、位置、频谱、诊断等专业参数。
- 新增设置界面 100% / 150% / 200% 缩放，只放大设置页文字与控件，不改变任何歌词字号；主屏与副屏的窗口尺寸、字号和透明度继续独立保存。
- 主屏与副屏位置改为按“屏幕 + 样式”分别记忆：切换经典、紧凑、Refined、AMLL、PiPWindow 等布局时不再共用同一坐标；恢复窗口时会按当前屏幕范围校正，减少换样式后跳位或跑出屏幕。
- 指定应用隐藏规则支持分别作用于主屏和副屏；显示参数页增加“锁定并触摸穿透”直接开关，无需依赖长按手势即可配置。
- 当前歌词与非当前歌词的描边可分别开启，并可独立设置颜色、不透明度和宽度；逐字与多行渲染路径统一应用对应描边参数。
- 顶部歌词条增加开机延迟恢复和挂载失败退避重试；水平偏移范围按实际屏幕宽度计算并限制在可见区域，宽屏车机上也能移动到两侧且不会完全移出屏幕。
- 修复部分车机在导航提示或临时媒体会话把播放位置短暂报为 0 时，歌词突然跳回开头的问题；同一歌曲会继续使用已有播放时钟，明确停止或出错时仍正常重置。
- 优化真实频谱与 Canvas 绘制的临时数组、路径和矩形复用，降低持续刷新时的对象分配与垃圾回收压力。

## 20260822-style-colors-and-sync-fixes

> 测试版：开发者未进行实际车机与播放场景测试，仅完成本地构建、单元测试与开发机安装验证；全样式颜色/描边、东风车机容错等改动的兼容性需要用户实机反馈。

- 当前/非当前歌词颜色扩展到全部悬浮窗样式：精致、Apple Music-like、PiPWindow 与纯净歌词现在与经典样式一样遵循"当前歌词颜色 / 非当前歌词颜色"设置，不再只有经典样式生效。
- 当前与非当前歌词颜色支持深浅环境成对设置：开启"跟随深浅环境"后可为当前/非当前歌词分别指定浅色与深色环境的颜色；未单独设置时沿用原有的单一自定义色。
- 歌词描边全面重做：描边现在覆盖纯净、精致、AMLL、PiPWindow 的多行与逐字歌词路径（此前仅单行文本生效），并新增描边颜色、不透明度、宽度（按字号百分比）三项自定义；自动描边色会按歌词亮度取反差色。
- 长按悬浮窗的快捷菜单改为按所在屏幕实际尺寸定位：悬浮窗贴边或靠上时菜单不再被裁剪出屏，"锁定并触摸穿透"等末尾选项始终可点。
- 紧凑歌词最小高度由 96/72dp 放宽到 64/48dp（启用播放控制按钮时保留控制行高度），小尺寸 HUD 摆放更自由。
- 统一纯净与 PiPWindow 的字号锚点：同一字号设置下两种样式的当前行字号一致（PiP 此前偏小约三成）。
- Android 4.4–6.0 车机修复快进后歌词不同步：播放器仅推送状态不含位置的事件时，改用外推位置重新锚定，不再把进度回退到旧值。
- 兼容部分车机系统资源损坏导致的 Toast 崩溃：所有提示统一走安全入口，弹提示失败只记日志不再闪退。
- 东风车机流媒体歌名锚定：车机播放器会把标题字段轮换成当前歌词句/词曲信息（如「词：小虫」「你就像那冬天里的一把火」），此前每次轮换都会清空已匹配歌词并显示“暂无匹配歌词”；现在播放中仅接受带结构特征（歌名 - 歌手、轨号前缀、音质标签）或来源切换的新标题，歌词句变异不再打断已显示的歌词。

## 20260822-dftc-transient-hold

> 测试版：同上一节，本节改动同样未经实机车机场景验证。

- 东风车机播放器读取增加瞬时容错：WecarFlow 短暂返回空歌名或未知状态时，保留最近一次有效会话并按原状态续显，不再让主/副屏歌曲信息每 600ms 闪烁；持续失败超过 15 秒才按无会话处理，播放器进程退出（binder 断开）时立即失效，不会残留幽灵会话。
- 活跃来源为东风车机播放器且在播时，系统侧闪现的临时媒体卡片不会抢走活跃槽位造成歌词反复重载；只有车机播放器停止报告播放、其他真实播放器开始播放，或车机会话快照超时后才会切换来源。
- 新增 `DftcMediaSessionReaderTest` 与 `MusicNotificationListenerTest` 覆盖保持窗口与仲裁规则。

## 20260820-ximalaya-and-dftc-media-test

> 测试版：开发者未进行实际设备与播放场景测试，仅完成本地构建和自动化检查；喜马拉雅及东风车机播放器的兼容性需要用户实机验证。

- 新增喜马拉雅与喜马拉雅车载版识别：从标准 MediaSession 读取当前音频信息，并针对标题中包含“歌手 - 歌名”的情况生成交叉词库查询，提高音乐类内容的歌词匹配率。
- 将东风车机播放器 `com.dftc.media` 作为独立来源适配：仅通过播放器 AIDL 获取标题、播放状态和控制能力，歌曲标题继续交由现有网易云、QQ、酷狗、酷我和汽水词库匹配歌词。
- 喜马拉雅与东风车机播放器使用独立来源标识和时间校正设置；停止直接注册 WecarFlow 客户端，避免干扰东风皓瀚仪表屏原生多媒体卡片与副屏投送链路。
- 增加多播放器优先级保护：车机播放器暂停时不会覆盖另一个正在使用的标准 MediaSession，车机专用读取失败也不会清空其他播放器歌词。

## 20260817-overlay-recovery-and-lyric-polish

- 新增“监听器自愈重连”：开机或仅恢复悬浮窗时会主动检查通知监听器并请求重连，减少重启后长期停留在“等待播放”的情况；副屏挂载失败会按退避策略自动重试，显示设备变更后也会重新尝试。
- 悬浮歌词新增“锁定位置但仍可交互”选项；长按改为快捷菜单，可直接进入尺寸与透明度、字体、频谱颜色设置，或分别锁定位置、开启触摸穿透。穿透解锁叉新增“自动弱化（2 秒）”和“自动隐藏（2 秒）”，自动隐藏后仍保留固定位置的点击解锁区域。
- 逐字歌词新增默认开启的“尾部拖长音重音”：仅对持续至少 1 秒且处于句末或标点前的词叠加轻微缩放与呼吸光晕，AMLL、紧凑、顶部歌词条和 Refined 样式共用该效果。
- 当前歌词与非当前歌词可分别设置颜色，并新增歌词文字描边开关；调色盘仅在圆形色轮内接收触摸，透明角落可正常滑动页面，且支持直接输入 `#RRGGBB` 或 `R,G,B` 色号。
- 经典三行歌词在无翻译时统一相邻行距；副屏可选择显示上一首、播放/暂停、下一首按钮；字体导入单文件上限由 24 MB 提升到 64 MB。
- 歌词缓存新增默认 30 天、永久保留、按容量自动淘汰三种策略，容量上限可在 16–512 MB 间设置；新增东风皓瀚播放器包名 `com.dftc.media` 的识别映射。
- 修复低版本车机兼容性：缓存容量淘汰不再调用 Android 7.0 才提供的排序 API，长按快捷菜单的阴影仅在 Android 5.0 及以上启用，最低 Android 4.4 设备可安全降级运行。

## 20260813-pure-lyrics-and-theme-compatibility

- 纯净歌词的“歌词显示行数”由最多 3 行扩展为最多 7 行，并按窗口高度自动收缩字号与行距；当前单句过长时会自动横向滚动，歌词区域仍可直接拖动悬浮窗。
- 纯净歌词新增主屏、副屏独立的“显示翻译”开关并默认开启；翻译显示在对应原文下方，空翻译和重复原文自动忽略，字号会按原文与翻译的总高度一起收缩以避免重叠。
- 修复顶部歌词“毛玻璃”在部分设备上仅叠加黑色滤镜的问题：仅在系统实际启用跨窗口模糊时使用真实模糊，不支持或关闭模糊时改用浅色半透明玻璃降级。
- 所有下拉列表统一跟随 Material 深浅主题，同时设置文字色和选项背景色；厂商主题返回无效颜色时按当前日间/夜间模式使用安全回退色，避免白底白字或暗底黑字。

## 20260813-bluetooth-local-lyrics-and-settings

- 新增蓝牙 AVRCP 歌曲识别：车机作为蓝牙音频接收端时，可读取手机提供的歌名、歌手和播放状态并进入现有歌词匹配流程；普通播放器暂时无会话时不会清空正在显示的蓝牙歌曲。
- 新增本地歌词匹配：优先查找歌曲同目录的 `.lrc` 文件，并支持授权一个音乐目录后按歌曲文件名、歌名或“歌手 - 歌名”继续匹配。
- 新增纯净歌词样式，只显示可调行数的歌词，不展示封面、歌曲信息、播放器信息和播放控件；轻触歌词区域也可按全局开关返回当前播放器。
- 新增独立底部频谱条，可固定在屏幕底边并复用主屏频谱样式；真实频谱支持低频率和高频率两种采集档位，暂停播放后自动释放采集，采集失败时自动回退虚拟律动。
- 修复顶部歌词条在真实频谱不可用时律动停止的问题；紧凑双行开启“翻译替代下一句”后，无翻译歌曲会重新显示下一句，不再退化为单行。
- 歌词背景支持白天、黑夜两套自定义颜色；歌名、歌手、播放器名称、歌词来源、歌词和频谱颜色统一迁移到“自定义颜色”页面，主屏、副屏和顶部歌词条仍可独立设置。
- 所有设置页的下拉选项统一使用适配深浅主题的文字与背景颜色；音乐读取、悬浮窗、录音频谱、蓝牙、通知显示、使用情况访问、安装更新和默认应用等入口集中到首页“使用权限”。
- 新增配置分享码：可填写简介后导出当前设置，也可通过分享码预览并导入；服务端分享记录具有有效期，并排除反馈、诊断、匿名身份、本地目录和字体文件等私有数据。
- 加强开机、亮屏、应用更新后的悬浮歌词恢复流程，兼容更多车机启动广播；新增“恢复默认设置”，可清除显示、歌词、启动、频谱、蓝牙、本地目录和字体设置，同时保留意见反馈、官方回复和匿名社区身份。
- 修复经典样式放大字号后与下一句、歌名或律动条重叠的问题：字号和行距会按窗口实际可用高度自动收缩，开启频谱时会预留底部空间。
- 修复经典样式窗口加宽、高度不变时歌词异常放大，以及换句滚动期间下一句固定贴底导致的错位重叠；下一句现在会与当前句同步滚动。

## 20260812-responsive-spectrum-lyrics

- 重构主界面为响应式 XML 布局：竖屏使用“首页、显示、歌词、系统”底部分类导航，横屏改为左侧导航；显示设置进一步拆分为悬浮歌词、启动与交互、副屏与位置、主副屏样式、主题字体与控件，并在横屏恢复双列卡片布局。
- 将律动/频谱能力扩展到全屏、主屏、副屏和顶部歌词条，提供柱状、镜像、胶囊、圆点与波形等预置样式；新增 HSL 彩虹、封面取色和自定义颜色，频谱与播放进度独立绘制。
- 修复深浅主题切换意外改变歌词颜色的问题；主屏、副屏可分别设置固定颜色或跟随深浅环境的两套颜色，只有开启“歌词跟随深浅色”后才显示深色/浅色选项。
- 播放控制按钮支持独立显示、大小及水平/垂直位置调节；全屏和悬浮窗关闭按钮支持常显、弱化或隐藏。
- 顶部歌词条新增真正的系统毛玻璃背景（Android 12 及以上，旧系统使用半透明降级），原封面柔化外观迁移为“紧凑单行背景”；支持独立跟随深浅环境并分别设置浅色、深色歌词颜色。
- 紧凑歌词第二行字号可调；顶部歌词条和紧凑歌词可用当前句翻译替代下一句，翻译会与本句共享走马灯位移，开启或关闭翻译时本句字号保持一致。
- 修复部分逐字歌词高亮层与底色层偶发错位：统一各样式的绘制基线，并同步清理 YRC 逐字片段与显示文本的首尾空白，避免时间轴字符边界偏移。

## 20260811-soda-share-lyrics

- 修复汽水音乐旧 PC 歌词接口返回空内容时无法匹配歌词的问题：手机版与车机版会根据应用包名分别使用对应的官方歌曲分享页，并从歌曲 ID 加载逐行、逐字时间轴。
- 汽水音乐 MediaSession 已提供实时歌词时无需等待全部在线词库查询结束即可显示；完整时间轴匹配成功后仍会自动切换为逐字歌词。
- 空响应不再触发 JSON 解析异常；新的分享页歌词缓存与旧接口空结果隔离，避免历史空缓存继续影响匹配。

## 20260811-netease-car-media-id

- 修复网易云音乐车机版使用十六进制媒体 ID 时可能误把其中开头数字当作歌曲 ID、加载错误歌词的问题；现在会改用歌名、歌手和时长进行准确匹配。

## 20260811-app-controls-and-lyrics

- 新增“开机 / 亮屏自启动悬浮窗”：记忆主屏、副屏和通知栏歌词；没有已记忆显示目标时自动保留主屏。关闭服务时会移除悬浮窗和音乐监听，保留自启动则进入最小待命以接收亮屏事件；关闭自启动才会完全停止该待命服务。
- 原“顶部歌词条”更名为“通知栏显示歌词”，继续使用桌面顶部透明双行歌词；旧系统通知卡片歌词与锁屏子选项暂时从主页隐藏，底层设置保持兼容。点击图标和自启动均会恢复已记忆的显示目标。
- 横屏双列按“显示与启动 / 歌词匹配”和“样式 / 支持”重新分组，改善车机界面的平衡与可读性。
- 新增“按词库强制匹配应用”：以网易云、QQ、酷狗、酷我、汽水词库为中心，为每个应用包名单独指定词库；未选应用继续使用默认匹配规则。
- 应用选择器支持应用名称和包名实时搜索；词库强制匹配与指定应用隐藏共用深色虚拟列表、可见项图标加载和短时缓存，避免大量应用时卡顿。
- 网易云音乐提示“该歌词不支持自动滚动”时明确显示“暂无匹配歌词”，不会误当作实时歌词或保留旧歌词。
- 重新匹配当前歌曲选择词库只作用于本次匹配，不会改写全局或应用规则；同时修复诊断中发现的歌词文字绘制栈溢出路径。

## 20260811-top-lyric-strip

- 主界面保持稳定的深色控制台；悬浮歌词可单独选择跟随系统、白天或夜晚环境，并可决定歌词颜色是否随该环境切换。
- 修复浅色环境中的颜色对比问题，更新弹窗、下拉选项和常见问题会使用 Material 主题色，避免出现难以辨认的黑色文字。
- 从“音乐读取权限”或“悬浮窗权限”设置页返回后仍未获得授权时，会提示前往常见问题查看设备/系统对应的解决方法。
- 新增独立的“顶部歌词条”开关：与系统通知歌词分离，在桌面顶部透明显示紧凑双行歌词，支持逐字高亮、长歌词跟随滚动和点击穿透。
- 顶部歌词条复用紧凑歌词渲染逻辑，隐藏封面、歌名歌手和律动/进度内容；支持字号、显示区域宽度与水平/垂直偏移的即时调整。
- 新增顶部歌词条详细设置与实时歌词预览，颜色、布局及字号调整会立即生效。
- 紧凑歌词相关选项迁移至独立的主屏/副屏详细设置页，仅在对应屏幕选中紧凑样式时显示。
- 新增可复用的圆形调色盘：拖动选择色相与饱和度，RGB 数值、滑块和色块预览双向同步；普通歌词、紧凑律动和顶部歌词共用。
- 调整顶部歌词条显示区域宽度时不再联动缩小字体，只改变左右显示范围与长歌词滚动区域。
- 美化“检查更新”弹窗：改为版本摘要卡片和“本次更新”正文，自动去除远端重复的“更新日志”标题。

## 20260808-feedback-controls

- 悬浮窗隐藏规则支持自定义选择应用；进入所选应用时隐藏主屏和副屏歌词，离开后自动恢复。
- 紧凑歌词样式支持按主屏、副屏分别开启双行显示，第二行显示下一句歌词。
- 重新匹配歌词时可手动修正歌名和歌手，改善本地或 U 盘音乐文件标签不准确导致的匹配失败。
- 本地与 U 盘歌曲在原始元数据未匹配时，会自动清理文件路径、音频扩展名、曲目序号和常见音质标记，并识别“歌手 - 歌名”或“歌名 - 歌手”文件名。
- 明确通知栏歌词显示在系统通知区域；Android 本地播放器需发布标准 MediaSession，歌词伴侣不承诺向 iPhone CarPlay 仪表盘发送媒体信息。
- 明确识别网易云音乐车机版包名 `com.netease.cloudmusic.iot`，并优先使用网易云词库。
- “关闭服务并退出”会完全停止悬浮窗、通知和后台服务，但保留主屏与副屏开关；下次点击图标时按两个开关同时恢复，并移除单独的图标启动目标。

## 20260806-overlay-lock-playback

- 修复在歌词区域长按锁定悬浮窗后，手动浏览状态未结束导致歌词停留在原位置、不再跟随播放进度的问题。

## 20260806-feedback-diagnostics

- 修复车机悬浮窗点击打开主界面时可能因后台启动限制导致的闪退，并增加通知入口兜底。
- 优化 Android 7.1 播放按钮绘制，复用图形资源，降低 `Path.finalize()` 超时风险。
- 反馈提交后生成 `feedbackId`，诊断快照支持关联反馈编号，便于定位问题。
- 增加车机安全使用须知，提供首次启用提示和设置页常驻入口。
- 支持主屏与副屏独立设置律动颜色。
- 支持重新匹配当前歌词，可切换自动、网易云、QQ、酷狗、酷我和汽水词库。
- 服务端增加 `feedbackId` 校验，并补充社区反馈接口测试。

## 20260806-service-exit

- 主页新增“关闭服务并退出”，会关闭悬浮窗、通知歌词和前台同步服务后退出应用。

## 20260806-launcher-overlay

- 修复紧凑单行背景不透明度设为 0 时可能出现的黑色残留区域。
- 新增图标启动悬浮窗开关，可记忆主屏/副屏启动目标；开启后 30 秒内再次点击图标进入主界面。

## 20260806-overlay-touch-through

- 悬浮窗双击可强制返回歌词伴侣；静止长按会锁定窗口并开启触摸穿透。
- 穿透状态保留圆形 × 解锁按钮，点击即可取消穿透并恢复拖动和窗口交互。

## 20260806-overlay-visibility

- 新增独立的“悬浮窗隐藏规则”页面，集中管理主屏悬浮窗和副屏歌词的自动隐藏行为。
- 新增“暂停或停止播放后隐藏”选项；恢复播放时会按照原有启用状态和保存位置自动重新显示悬浮窗。
- 新增“进入当前播放器后隐藏”选项；通过系统“使用情况访问”识别当前媒体会话所属播放器，离开播放器后自动恢复，未授权时不会误隐藏。

## 20260805-fullscreen

- 新增全屏歌词展示：直接按照 Apple Music-like Lyrics、Refined Now Playing 与 PiPWindow 的开源源码移植横竖屏布局比例、封面尺寸、歌词字号、行级淡化和 Canvas 坐标规则。
- 全屏右上角新增独立退出按钮，同时保留系统返回键；移除全屏长按退出，避免浏览或拖动歌词时误退出。
- 悬浮窗最大宽高放宽到目标屏幕的完整分辨率，主屏与副屏都可铺满显示；副屏尺寸和 Refined 参数调整后立即刷新。
- 静止长按悬浮窗并抬手后会关闭当前悬浮窗并返回歌词伴侣；拖动、多指或取消手势会永久取消本次长按，不再误进入应用。
- 封面、歌名与歌手字号支持独立调整；主屏和副屏字号上限提高，下一句歌词和 Refined 歌词可使用更大字号。
- 新增跟随系统、白天、夜晚三种歌词主题模式，切换夜间主题时不再需要逐项修改字体颜色。
- 逐字歌词高亮改为字符内部连续插值，AMLL、Refined、PiPWindow 等样式的逐字推进更加平滑。

## 20260804-de73dcd

- 新增 Apple Music-like Lyrics 样式：提供基于封面取色的沉浸式动态背景、横屏封面与歌词双栏、窄屏歌词主视图、大字号逐字浮起高亮、远近行模糊淡出、弹性换行与三点间奏；主屏和副屏可独立选择，并标注 AMLL 开源仓库与许可。
- 新增启动公告：显示歌词伴侣反馈交流群 `1049772727`，可临时关闭或选择不再提示；公告和更新提示的标题采用黑色。
- 诊断快照补充通知读取、播放会话、歌词匹配、时间轴、偏移和主副屏显示状态，便于定位“歌词对不上”和旧车机播放器兼容问题；仍不上传歌曲名、歌手、歌词、账号信息或全局 logcat。
- 紧凑单行底部律动条升级为 32 段高频谱：可分别为主屏、副屏选择真实系统音频律动；关闭后保留无需录音权限的平滑虚拟律动。
- 真实律动会在首次使用时请求录音权限；设备/ROM 不支持系统输出采集或未授权时保持静态待机条，不伪造频谱动画。
- 修复真实频谱低频端没有 FFT 采样点时左侧柱体空缺的问题，并加入平滑进度亮度过渡与贴底的连续播放进度线。
- 新增“轻触悬浮窗返回播放器”开关：开启后优先回到当前播放应用，关闭时仍进入歌词伴侣。
- 将播放器把当前歌词写入标题的保护扩展到酷狗及所有 MediaSession 音乐播放器；仍会以曲目 ID、歌手和时长变化识别真实换歌。
- 新增歌词颜色自动/手动模式：自动模式跟随当前样式；手动模式提供 RGB 滑杆和颜色预览，主屏、副屏可分别保存并实时应用到当前、前后及翻译歌词。
- 修复主屏三个播放控制都关闭后，紧凑单行样式仍残留控制栏阴影的问题。
- 紧凑单行样式在右侧封面下方显示歌曲名称和歌手；关闭封面时会一并隐藏这些歌曲信息。
- 修复 Refined 背景模糊将过小封面缩略图放大后出现网格的问题；改为缓存平滑模糊图，减少歌词播放时的重复处理。
- 优化歌词换行起始动画：改为跟随屏幕 VSync 刷新，主屏默认与 Refined 样式都能更平滑地进入下一行；流体和动态渐变背景的动画刷新同步提升。
- 修复部分 Android 11 厂商系统缺少悬浮窗权限页或文件选择器时的闪退；会逐级打开可用设置页并给出手动操作提示。
- 新增下一句独立字号、不透明度与平滑滚动换句开关；默认与精致样式均支持平滑换句。
- 歌词时间校正保留全局基准，并新增网易云、QQ、酷狗、酷我、汽水和其他播放器的独立附加校正。
- 修复 QQ 音乐部分版本将当前歌词写入歌曲标题时被误判为换歌的问题；保留原歌曲信息，词库未命中时可显示 QQ 实时歌词。
- 收到反馈回复后，重新打开应用会自动弹出未读回复；已查看的回复不会重复打扰，仍可在设置中查看历史回复。
- 主屏和副屏的显示参数改为独立存储，并拆分为两个单独的设置入口，减少主页设置页的堆叠。
- 副屏紧凑单行模式可分别关闭封面与底部律动条。
- 主屏悬浮窗的上一首、暂停/播放、下一首按键可分别显示或隐藏；隐藏后不再响应对应触控。
- Refined 样式详细设置也按主屏和副屏独立保存。
- 新增网易云、QQ 音乐、酷狗、酷我多词库并行匹配与播放器同源优先。
- 新增主屏悬浮窗、副屏原生 Display 渲染、自定义布局与位置摇杆。
- 新增 Refined、PiPWindow 风格及横屏双列设置界面。
- 新增可配置词库优先级和播放器同源词库回退策略。
- 新增 GitHub Actions 自动签名 Release、App 内更新检查与独立更新服务器。
- 新增 QQ 音乐 QRC、酷狗 KRC 逐字时间轴，并补全多词库翻译歌词对齐。
- 改进 Refined 间奏动画、长歌词滚动和可拖动歌词预览，支持惯性与可中断回弹。
- 优化 Canvas 刷新调度、硬件加速、文本布局缓存和封面采样解码，降低渲染开销。
- 新增匿名实时在线人数和 App 内意见反馈入口，配套服务器心跳、限流与反馈落盘接口。
- 更新服务器默认端口调整为 8790，并修复 HTTPS 反向代理生成带 `:80` 下载链接的问题。
